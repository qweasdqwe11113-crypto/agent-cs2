# CS2 Demo Parser

Go command-line adapter around `demoinfocs-golang/v5`.

It accepts a CS2 `.dem` path and writes a normalized JSON document to standard output. The TypeScript Worker invokes this executable after safe ZIP extraction.
